openapi: 3.1.0
info:
  title: Gugu Admin API
  version: 0.1.0
  description: |
    SKU 적재, SKU snapshot 갱신, AliExpress 토큰 관리를 위한 어드민 배치 API 명세입니다.
    2026-04-18 기준으로 상품(product) 레벨 가격 파이프라인은 제거되었고, 가격 정본은 SKU 계층(`sku_price_history`, `sku_price_snapshot`)만 사용합니다.
servers:
  - url: https://lees-admin.duckdns.org
    description: 운영 서버
  - url: http://localhost:8700
    description: 로컬 개발 서버
tags:
  - name: system
    description: 시스템 엔드포인트
  - name: batch
    description: 어드민 배치 엔드포인트
  - name: product
    description: 어드민 상품 조회 엔드포인트
  - name: user
    description: 어드민 사용자/세션 관리 엔드포인트
  - name: admin-auth
    description: 어드민 로그인 및 액세스 토큰 발급
  - name: token
    description: AliExpress OAuth 토큰 관리
paths:
  /health:
    get:
      tags:
        - system
      summary: 헬스 체크
      operationId: healthCheck
      responses:
        '200':
          description: 서버 정상 동작 중
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
  /v1/admin/auth/login:
    post:
      tags:
        - admin-auth
      security: []
      summary: 관리자 로그인
      description: |
        `admin_user.login_id/password`로 로그인하고 관리자 액세스 토큰(Bearer)을 발급합니다.
        회원가입 API는 제공하지 않으며, 계정은 운영자가 DB에 수동으로 생성합니다.
      operationId: adminLogin
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminLoginRequest'
      responses:
        '200':
          description: 로그인 성공
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminLoginResponse'
        '400':
          description: 요청 본문 누락/형식 오류
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: 인증 실패(id/password 불일치)
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: 로그인 처리 실패
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/batch/load-hot-products:
    post:
      tags:
        - batch
      security:
        - AdminApiKey: []
        - AdminBearer: []
      summary: 인기 상품 적재
      description: |
        AliExpress 인기 상품 메타데이터를 대표 통화(KRW) 기준으로 조회해 `product`/`product_external_alias`에 동기화합니다.
        이 엔드포인트는 가격을 저장하지 않습니다. 가격은 `POST /v1/batch/update-sku-snapshots`에서 SKU 기준으로만 수집됩니다.
        이미 존재하는 상품은 메타/alias만 갱신합니다.
      operationId: loadHotProducts
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HotProductLoadRequest'
      responses:
        '202':
          description: 인기 상품 적재 작업 시작됨
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AcceptedMessageResponse'
        '400':
          description: 잘못된 요청
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: 관리자 인증 정보 누락 또는 오류
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/batch/enrich-skus/hot-products:
    post:
      tags:
        - batch
      security:
        - AdminApiKey: []
        - AdminBearer: []
      summary: 인기 상품 SKU 보강
      description: SKU가 없는 인기 상품에 대해 Dropshipping API로 SKU를 적재합니다.
      operationId: enrichHotProducts
      responses:
        '202':
          description: SKU 보강 작업 시작됨
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AcceptedMessageResponse'
        '401':
          description: 관리자 인증 정보 누락 또는 오류
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/batch/enrich-skus/all:
    post:
      tags:
        - batch
      security:
        - AdminApiKey: []
        - AdminBearer: []
      summary: 전체 상품 SKU 보강
      description: 모든 상품에 대해 Dropshipping API로 SKU를 적재합니다.
      operationId: enrichAllProducts
      responses:
        '202':
          description: SKU 보강 작업 시작됨
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AcceptedMessageResponse'
        '401':
          description: 관리자 인증 정보 누락 또는 오류
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/batch/update-product-prices:
    post:
      tags:
        - batch
      security:
        - AdminApiKey: []
        - AdminBearer: []
      deprecated: true
      summary: (Deprecated) 상품 가격 갱신 배치
      description: |
        2026-04-18부터 product-level 가격 파이프라인은 제거되었습니다.
        SKU 가격 수집은 `POST /v1/batch/update-sku-snapshots`를 사용하세요.
      operationId: updateProductPrices
      responses:
        '410':
          description: Deprecated endpoint (더 이상 지원하지 않음)
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                status: error
                error:
                  code: DEPRECATED_ENDPOINT
                  message: update-product-prices is deprecated; use /v1/batch/update-sku-snapshots
        '401':
          description: 관리자 인증 정보 누락 또는 오류
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/batch/update-sku-snapshots:
    post:
      tags:
        - batch
      security:
        - AdminApiKey: []
        - AdminBearer: []
      summary: SKU snapshot 갱신 배치
      description: |
        대상 상품들의 SKU 가격을 AliExpress Dropshipping API에서 다시 조회하여 `sku_price_snapshot`을 갱신합니다.
        SKU 가격이 직전 이력과 다를 때만 `sku_price_history`를 기록합니다.
        `target_group` 의미는 다음과 같습니다: `ALL`(전체), `HOT_PRODUCTS`(`collection_source=HOT_PRODUCT_QUERY`), `TRACKED`(`user_tracked_item.deleted_at IS NULL` 기준 distinct product_id).
        `product_ids`가 주어지면 `target_group`보다 우선합니다.
        저사양 단일 서버 운영 시에는 통화를 직렬로 실행하는 것을 권장합니다.
      operationId: updateSkuSnapshots
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PriceUpdateRequest'
            examples:
              phase1KRWAll:
                summary: Phase 1 - KRW 전체
                value:
                  currencies:
                    - KRW
                  target_group: ALL
                  requested_by: scheduler
              phase1USDHot:
                summary: Phase 1 - USD 인기상품
                value:
                  currencies:
                    - USD
                  target_group: HOT_PRODUCTS
                  requested_by: scheduler
              phase1USDTracked:
                summary: Phase 1 - USD 추적상품
                value:
                  currencies:
                    - USD
                  target_group: TRACKED
                  requested_by: scheduler
              allProductsDefault:
                summary: 기본값 사용(ALL + KRW)
                value: {}
              trackedOnly:
                summary: 수동 실행 - 추적 상품만
                value:
                  target_group: TRACKED
                  currencies:
                    - USD
                  requested_by: admin
              filteredByIds:
                summary: 특정 상품만
                value:
                  product_ids:
                    - product-id-1
                    - product-id-2
                  currencies:
                    - USD
                  requested_by: admin
      responses:
        '202':
          description: SKU snapshot 갱신 배치 시작됨
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PriceUpdateAcceptedResponse'
        '400':
          description: 잘못된 요청
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: 관리자 인증 정보 누락 또는 오류
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: 프리뷰 실패
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/products:
    get:
      tags:
        - product
      security:
        - AdminApiKey: []
        - AdminBearer: []
      summary: 상품 목록 조회
      description: |
        `product`/`product_localization` 기준 메타 정보를 반환합니다.
        가격 필드(`current_price`, `currency`)는 의도적으로 포함하지 않습니다.
        상세 진입은 `product_url`(AliExpress 링크) 중심 UX를 전제로 합니다.
      operationId: listProducts
      parameters:
        - in: query
          name: language
          required: false
          schema:
            type: string
            enum:
              - KO
              - EN
            default: KO
          description: 조회 언어. KO면 한국어, EN이면 영어 localization을 우선 사용합니다.
        - in: query
          name: collection_source
          required: false
          schema:
            type: string
          description: 수집 경로 필터
      responses:
        '200':
          description: 상품 목록
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductListResponse'
        '401':
          description: 관리자 인증 정보 누락 또는 오류
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: 상품 목록 조회 실패
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/batch/status:
    get:
      tags:
        - batch
      security:
        - AdminApiKey: []
        - AdminBearer: []
      summary: 배치 상태 조회
      description: |
        최근 실행된 배치 작업 상태를 반환합니다.
        현재 조회 가능한 작업 유형은 `SKU_SNAPSHOT_UPDATE` 하나입니다.
      operationId: getBatchStatus
      parameters:
        - in: query
          name: job_type
          required: false
          schema:
            type: string
            enum:
              - SKU_SNAPSHOT_UPDATE
          description: 생략하면 기본값 `SKU_SNAPSHOT_UPDATE`로 조회합니다.
      responses:
        '200':
          description: 배치 상태
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BatchStatusResponse'
        '400':
          description: 잘못된 작업 유형
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: 관리자 인증 정보 누락 또는 오류
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/aliexpress/token/generate:
    post:
      tags:
        - token
      security:
        - AdminApiKey: []
        - AdminBearer: []
      summary: OAuth 코드로 토큰 발급
      description: |
        AliExpress 셀러 인증 콜백에서 받은 OAuth 코드를 access/refresh 토큰으로 교환합니다.
        발급된 토큰은 DB에 저장됩니다.
      operationId: generateToken
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TokenGenerateRequest'
            examples:
              affiliate:
                summary: Affiliate 토큰 발급
                value:
                  code: '3_500102_JxZ05Ux3cnnSSUm6dCxYg6Q26'
                  app_type: AFFILIATE
              dropshipping:
                summary: Dropshipping 토큰 발급
                value:
                  code: '3_500102_abc123'
                  app_type: DROPSHIPPING
      responses:
        '200':
          description: 토큰 발급 및 저장 완료
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenResultResponse'
        '400':
          description: 잘못된 요청 또는 미지원 app_type
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: 관리자 인증 정보 누락 또는 오류
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: 토큰 발급 또는 저장 실패
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/aliexpress/oauth/authorize-url:
    get:
      tags:
        - token
      security:
        - AdminApiKey: []
        - AdminBearer: []
      summary: OAuth 인가 URL 생성
      description: |
        지정한 app_type에 대해 AliExpress seller authorization URL을 생성합니다.
        반환된 URL로 이동하면 callback 엔드포인트에서 code를 내부적으로 받아 바로 토큰을 저장합니다.
      operationId: getAliExpressAuthorizeUrl
      parameters:
        - in: query
          name: app_type
          required: true
          schema:
            type: string
            enum:
              - AFFILIATE
              - DROPSHIPPING
      responses:
        '200':
          description: 인가 URL 생성 성공
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/APIResponse'
        '400':
          description: 잘못된 앱 유형
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: 관리자 인증 정보 누락 또는 오류
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/aliexpress/oauth/callback/affiliate:
    get:
      tags:
        - token
      summary: Affiliate OAuth callback
      description: AliExpress Affiliate 앱 authorization code를 받아 내부 토큰 저장까지 수행합니다.
      operationId: aliexpressAffiliateCallback
      parameters:
        - in: query
          name: code
          required: true
          schema:
            type: string
      responses:
        '200':
          description: 토큰 저장 성공
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
        '400':
          description: code 누락 또는 잘못된 요청
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
        '500':
          description: 토큰 저장 실패
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
  /v1/aliexpress/oauth/callback/dropshipping:
    get:
      tags:
        - token
      summary: Dropshipping OAuth callback
      description: AliExpress Dropshipping 앱 authorization code를 받아 내부 토큰 저장까지 수행합니다.
      operationId: aliexpressDropshippingCallback
      parameters:
        - in: query
          name: code
          required: true
          schema:
            type: string
      responses:
        '200':
          description: 토큰 저장 성공
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
        '400':
          description: code 누락 또는 잘못된 요청
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
        '500':
          description: 토큰 저장 실패
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
  /v1/aliexpress/token/refresh:
    post:
      tags:
        - token
      security:
        - AdminApiKey: []
        - AdminBearer: []
      summary: 토큰 수동 갱신
      description: |
        저장된 refresh_token을 사용하여 access_token을 갱신합니다.
        `app_type`을 지정하면 해당 앱만 갱신하고, 생략하면 AFFILIATE/DROPSHIPPING 둘 다 점검합니다.
        보통 스케줄러가 자동으로 처리하지만 수동 트리거도 가능합니다.
      operationId: refreshToken
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TokenRefreshRequest'
            examples:
              affiliate:
                summary: Affiliate 토큰 갱신
                value:
                  app_type: AFFILIATE
              allTypes:
                summary: 모든 앱 토큰 점검
                value: {}
      responses:
        '200':
          description: 토큰 갱신 및 저장 완료
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenResultResponse'
        '400':
          description: 잘못된 요청 또는 미지원 app_type
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: 해당 app_type의 토큰이 없음
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: 관리자 인증 정보 누락 또는 오류
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: 토큰 갱신 또는 저장 실패
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/aliexpress/token/status:
    get:
      tags:
        - token
      security:
        - AdminApiKey: []
        - AdminBearer: []
      summary: 토큰 상태 조회
      description: |
        저장된 토큰의 현재 상태를 반환합니다.
        app_type 파라미터 없이 호출하면 전체 앱 유형의 상태를 반환합니다.
      operationId: getTokenStatus
      parameters:
        - in: query
          name: app_type
          required: false
          schema:
            type: string
            enum:
              - AFFILIATE
              - DROPSHIPPING
      responses:
        '200':
          description: 토큰 상태
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenStatusResponse'
        '500':
          description: 토큰 조회 실패
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: 관리자 인증 정보 누락 또는 오류
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/users:
    get:
      tags:
        - user
      security:
        - AdminApiKey: []
        - AdminBearer: []
      summary: 사용자 목록 조회
      operationId: listUsers
      parameters:
        - in: query
          name: search
          required: false
          schema:
            type: string
        - in: query
          name: plan
          required: false
          schema:
            type: string
            enum:
              - FREE
        - in: query
          name: status
          required: false
          schema:
            type: string
            enum:
              - ACTIVE
              - INACTIVE
        - in: query
          name: page
          required: false
          schema:
            type: integer
            default: 1
        - in: query
          name: page_size
          required: false
          schema:
            type: integer
            default: 20
      responses:
        '200':
          description: 사용자 목록
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserListResponse'
        '500':
          description: 사용자 목록 조회 실패
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: 관리자 인증 정보 누락 또는 오류
          headers:
            Cache-Control:
              $ref: '#/components/headers/Cache-Control'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/users/{user_id}/sessions:
    get:
      tags:
        - user
      security:
        - AdminApiKey: []
        - AdminBearer: []
      summary: 사용자 세션 목록 조회
      operationId: listUserSessions
      parameters:
        - in: path
          name: user_id
          required: true
          schema:
            type: string
        - in: query
          name: status
          required: false
          schema:
            type: string
            enum:
              - ACTIVE
              - INACTIVE
        - in: query
          name: revoked
          required: false
          schema:
            type: boolean
        - in: query
          name: reuse_detected
          required: false
          schema:
            type: boolean
      responses:
        '200':
          description: 사용자 세션 목록
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSessionListResponse'
        '400':
          description: 잘못된 요청
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: 관리자 인증 정보 누락 또는 오류
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: 사용자 세션 조회 실패
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/users/{user_id}/sessions/revoke:
    post:
      tags:
        - user
      security:
        - AdminApiKey: []
        - AdminBearer: []
      summary: 사용자 전체 세션 강제 로그아웃
      operationId: revokeAllUserSessions
      parameters:
        - in: path
          name: user_id
          required: true
          schema:
            type: string
      responses:
        '200':
          description: 세션 강제 로그아웃 완료
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSessionRevokeResponse'
        '400':
          description: 잘못된 요청
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: 관리자 인증 정보 누락 또는 오류
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: 세션 강제 로그아웃 실패
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/users/{user_id}/sessions/{session_id}/revoke:
    post:
      tags:
        - user
      security:
        - AdminApiKey: []
        - AdminBearer: []
      summary: 특정 세션 강제 로그아웃
      operationId: revokeUserSessionByID
      parameters:
        - in: path
          name: user_id
          required: true
          schema:
            type: string
        - in: path
          name: session_id
          required: true
          schema:
            type: string
      responses:
        '200':
          description: 특정 세션 강제 로그아웃 완료
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSessionRevokeResponse'
        '400':
          description: 잘못된 요청
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: 관리자 인증 정보 누락 또는 오류
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: 세션 미존재 또는 이미 revoked
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: 세션 강제 로그아웃 실패
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/users/{user_id}/sessions/token-families/{token_family_id}/revoke:
    post:
      tags:
        - user
      security:
        - AdminApiKey: []
        - AdminBearer: []
      summary: 토큰 패밀리 단위 세션 강제 로그아웃
      operationId: revokeUserSessionsByTokenFamily
      parameters:
        - in: path
          name: user_id
          required: true
          schema:
            type: string
        - in: path
          name: token_family_id
          required: true
          schema:
            type: string
      responses:
        '200':
          description: 패밀리 단위 세션 강제 로그아웃 완료
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSessionRevokeResponse'
        '400':
          description: 잘못된 요청
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: 관리자 인증 정보 누락 또는 오류
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: 패밀리 단위 세션 강제 로그아웃 실패
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
components:
  headers:
    Cache-Control:
      description: 캐시를 비활성화하는 표준 헤더
      schema:
        type: string
        example: no-store, no-cache, must-revalidate
    Pragma:
      description: HTTP/1.0 클라이언트 캐시를 비활성화하는 호환성 헤더
      schema:
        type: string
        example: no-cache
    Expires:
      description: 응답이 즉시 만료되었음을 나타내는 헤더
      schema:
        type: string
        example: '0'
  securitySchemes:
    AdminApiKey:
      type: apiKey
      in: header
      name: X-Admin-API-Key
      description: |
        모든 `/v1` 어드민 API 호출 시 필요한 헤더.
        `ADMIN_API_KEYS`가 설정되면 해당 값 중 하나와 일치해야 하며, 비어 있으면 하위호환을 위해 non-empty 값만 확인합니다.
    AdminBearer:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: |
        `POST /v1/admin/auth/login`으로 발급한 관리자 액세스 토큰.
        `Authorization: Bearer <token>` 형식으로 전송합니다.
  schemas:
    HealthResponse:
      type: object
      required:
        - status
      properties:
        status:
          type: string
          example: ok
    APIResponse:
      type: object
      required:
        - status
      properties:
        status:
          type: string
          example: success
        data:
          description: 성공 응답 데이터
        error:
          $ref: '#/components/schemas/ErrorPayload'
    ErrorPayload:
      type: object
      required:
        - code
        - message
      properties:
        code:
          type: string
        message:
          type: string
    ErrorResponse:
      allOf:
        - $ref: '#/components/schemas/APIResponse'
        - type: object
          properties:
            status:
              type: string
              enum:
                - error
            error:
              $ref: '#/components/schemas/ErrorPayload'
    AcceptedMessageResponse:
      allOf:
        - $ref: '#/components/schemas/APIResponse'
        - type: object
          properties:
            data:
              type: object
              required:
                - message
              properties:
                message:
                  type: string
    AdminLoginRequest:
      type: object
      required:
        - id
        - password
      properties:
        id:
          type: string
          description: 관리자 로그인 ID (`admin_user.login_id`)
          example: super-admin
        password:
          type: string
          format: password
          description: 관리자 로그인 비밀번호
          example: my-admin-password
    AdminLoginResponse:
      allOf:
        - $ref: '#/components/schemas/APIResponse'
        - type: object
          properties:
            data:
              type: object
              required:
                - admin_id
                - login_id
                - access_token
                - token_type
                - expires_at
              properties:
                admin_id:
                  type: string
                  description: 로그인한 관리자 내부 PK (`admin_user.id`)
                login_id:
                  type: string
                  description: 로그인에 사용한 관리자 ID (`admin_user.login_id`)
                access_token:
                  type: string
                  description: 관리자 Bearer 액세스 토큰
                token_type:
                  type: string
                  enum:
                    - Bearer
                expires_at:
                  type: string
                  format: date-time
    PriceUpdateRequest:
      type: object
      description: |
        `POST /v1/batch/update-sku-snapshots` 요청 본문.
        가격 수집 단위는 SKU이며, product-level 가격은 저장하지 않습니다.
      properties:
        collection_source:
          type: string
          description: 추가 필터. `target_group` 적용 후 다시 좁힐 때 사용합니다.
          example: HOT_PRODUCT_QUERY
        market:
          type: string
          description: 추가 마켓 필터
          enum:
            - ALIEXPRESS
            - COUPANG
        product_ids:
          type: array
          description: 특정 내부 `product.id` 목록(`external_product_id`가 아님). 값이 있으면 `target_group`보다 우선합니다.
          items:
            type: string
        currencies:
          type: array
          description: |
            조회/적재 대상 통화 목록.
            미지정 시 기본값은 `KRW`입니다.
            저사양 환경에서는 `KRW`/`USD`를 한 요청에서 동시에 넣기보다 분리 실행을 권장합니다.
          items:
            type: string
            enum:
              - KRW
              - USD
        target_group:
          type: string
          description: |
            대상 상품 그룹.
            `ALL`: 전체 후보, `HOT_PRODUCTS`: `collection_source=HOT_PRODUCT_QUERY`, `TRACKED`: `user_tracked_item.deleted_at IS NULL`의 distinct `product_id`.
          enum:
            - ALL
            - HOT_PRODUCTS
            - TRACKED
          default: ALL
        collected_before:
          type: string
          description: 이 시각 이전에 수집된 상품만 대상으로 제한
          format: date-time
        force:
          type: boolean
          description: 강제 갱신 플래그(향후 확장용 필드)
        requested_by:
          type: string
          description: 요청자 식별자(로그/추적용)
      example:
        currencies:
          - KRW
        target_group: ALL
        requested_by: scheduler
    HotProductLoadRequest:
      type: object
      properties:
        category_ids:
          type: array
          description: 카테고리 ID 목록
          items:
            type: string
        keywords:
          type: string
          description: 검색 키워드
        sort:
          type: string
          description: 정렬 기준
        min_sale_price:
          type: string
          description: 최소 가격
        max_sale_price:
          type: string
          description: 최대 가격
    HotProductLoadResponse:
      allOf:
        - $ref: '#/components/schemas/APIResponse'
        - type: object
          properties:
            data:
              type: object
              required:
                - requested_count
                - hot_product_saved
                - product_saved_count
                - skipped_count
              properties:
                requested_count:
                  type: integer
                  description: API에서 조회된 상품 수
                processed_pages:
                  type: integer
                  description: 처리된 페이지 수
                hot_product_saved:
                  type: integer
                  description: 처리된 hot product item 수
                product_saved_count:
                  type: integer
                  description: product 테이블에 신규 저장된 수
                skipped_count:
                  type: integer
                  description: 이미 존재하여 건너뛴 상품 수
    PriceUpdateAcceptedResponse:
      allOf:
        - $ref: '#/components/schemas/APIResponse'
        - type: object
          properties:
            data:
              type: object
              required:
                - message
                - job
              properties:
                message:
                  type: string
                job:
                  $ref: '#/components/schemas/BatchJobStatus'
    ProductListResponse:
      allOf:
        - $ref: '#/components/schemas/APIResponse'
        - type: object
          properties:
            data:
              type: object
              required:
                - language
                - items
              properties:
                language:
                  type: string
                  enum:
                    - KO
                    - EN
                items:
                  type: array
                  items:
                    $ref: '#/components/schemas/ProductListItem'
    ProductListItem:
      type: object
      required:
        - id
        - market
        - external_product_id
        - original_url
        - title
        - main_image_url
        - product_url
        - collection_source
        - language
      properties:
        id:
          type: string
          description: 내부 상품 ID
        market:
          type: string
          enum:
            - ALIEXPRESS
            - COUPANG
          description: 마켓
        external_product_id:
          type: string
          description: 원천 마켓 상품 ID
        original_url:
          type: string
          description: 원천 URL
        title:
          type: string
          description: 상품 제목(요청 language 우선, 없으면 기본값)
        main_image_url:
          type: string
          description: 대표 이미지 URL(요청 language 우선, 없으면 기본값)
        product_url:
          type: string
          description: 상세 이동 URL(클릭 시 외부 마켓 링크로 이동)
        collection_source:
          type: string
          description: 수집 경로
        language:
          type: string
          enum:
            - KO
            - EN
          description: 응답 로컬라이제이션 언어
    BatchStatusResponse:
      allOf:
        - $ref: '#/components/schemas/APIResponse'
        - type: object
          properties:
            data:
              oneOf:
                - $ref: '#/components/schemas/BatchJobStatus'
                - type: object
                  properties:
                    job_type:
                      type: string
                    status:
                      type:
                        - string
                        - 'null'
    BatchJobStatus:
      type: object
      description: SKU snapshot 배치의 최근 상태 스냅샷
      required:
        - job_type
        - status
        - trigger_type
        - filter
        - requested_at
        - total_count
        - success_count
        - fail_count
        - skipped_count
      properties:
        job_type:
          type: string
          enum:
            - SKU_SNAPSHOT_UPDATE
        status:
          type: string
          enum:
            - QUEUED
            - RUNNING
            - COMPLETED
            - FAILED
        trigger_type:
          type: string
          enum:
            - MANUAL
            - SCHEDULED
        filter:
          $ref: '#/components/schemas/PriceUpdateFilter'
        requested_at:
          type: string
          format: date-time
        started_at:
          type:
            - string
            - 'null'
          format: date-time
        finished_at:
          type:
            - string
            - 'null'
          format: date-time
        total_count:
          type: integer
          description: 이번 실행에서 평가된 전체 대상 상품 수
        success_count:
          type: integer
          description: 1개 이상 SKU snapshot 기록에 성공한 상품 수
        fail_count:
          type: integer
          description: API/기록 오류로 처리 실패한 상품 수
        skipped_count:
          type: integer
          description: 처리할 SKU가 없거나 저장 대상이 없어 건너뛴 상품 수
        last_error:
          type: string
          description: 마지막 오류 메시지(없으면 빈 값)
    PriceUpdateFilter:
      type: object
      description: 실행 당시 적용된 필터 스냅샷
      properties:
        collection_source:
          type: string
          description: 수집 경로 추가 필터
        market:
          type: string
          enum:
            - ALIEXPRESS
            - COUPANG
          description: 마켓 추가 필터
        product_ids:
          type: array
          description: 명시적 대상 product.id 목록
          items:
            type: string
        currencies:
          type: array
          description: 실행 통화 목록
          items:
            type: string
            enum:
              - KRW
              - USD
        target_group:
          type: string
          description: 대상 그룹
          enum:
            - ALL
            - HOT_PRODUCTS
            - TRACKED
        collected_before:
          type:
            - string
            - 'null'
          format: date-time
          description: 수집시각 상한 필터
        force:
          type: boolean
          description: 강제 갱신 플래그
    TokenGenerateRequest:
      type: object
      required:
        - code
        - app_type
      properties:
        code:
          type: string
          description: AliExpress 콜백에서 받은 OAuth 인증 코드
        app_type:
          type: string
          enum:
            - AFFILIATE
            - DROPSHIPPING
          description: 토큰을 발급할 AliExpress 앱 유형
    TokenRefreshRequest:
      type: object
      required:
        - app_type
      properties:
        app_type:
          type: string
          enum:
            - AFFILIATE
            - DROPSHIPPING
    TokenResultResponse:
      allOf:
        - $ref: '#/components/schemas/APIResponse'
        - type: object
          properties:
            data:
              type: object
              required:
                - seller_id
                - app_type
                - access_token_expires_at
              properties:
                seller_id:
                  type: string
                app_type:
                  type: string
                  enum:
                    - AFFILIATE
                    - DROPSHIPPING
                access_token_expires_at:
                  type: string
                  format: date-time
                refresh_token_expires_at:
                  type:
                    - string
                    - 'null'
                  format: date-time
    TokenStatusResponse:
      allOf:
        - $ref: '#/components/schemas/APIResponse'
        - type: object
          properties:
            data:
              type: object
              required:
                - tokens
              properties:
                tokens:
                  type: array
                  items:
                    $ref: '#/components/schemas/TokenStatusEntry'
    TokenStatusEntry:
      type: object
      required:
        - app_type
        - status
      properties:
        app_type:
          type: string
          enum:
            - AFFILIATE
            - DROPSHIPPING
        seller_id:
          type: string
        user_nick:
          type: string
        status:
          type: string
          description: 'ACTIVE: 정상 / EXPIRING_SOON: 곧 만료 / EXPIRED: 만료됨 / NOT_FOUND: 토큰 없음'
          enum:
            - ACTIVE
            - EXPIRING_SOON
            - EXPIRED
            - NOT_FOUND
        access_token_expires_at:
          type: string
          format: date-time
        refresh_token_expires_at:
          type:
            - string
            - 'null'
          format: date-time
        last_refreshed_at:
          type: string
          format: date-time
    AdminUser:
      type: object
      required:
        - id
        - email
        - display_name
        - plan
        - status
        - email_verified
        - tracked_item_count
        - created_at
        - sessions
      properties:
        id:
          type: string
        email:
          type: string
        display_name:
          type: string
        plan:
          type: string
          enum:
            - FREE
        status:
          type: string
          enum:
            - ACTIVE
            - INACTIVE
        email_verified:
          type: boolean
        tracked_item_count:
          type: integer
        created_at:
          type: string
          format: date-time
        last_login_at:
          type:
            - string
            - 'null'
          format: date-time
        sessions:
          type: array
          items:
            $ref: '#/components/schemas/AdminUserLoginSession'
    AdminUserLoginSession:
      type: object
      required:
        - id
        - user_id
        - status
        - status_reason
        - refresh_token_hash
        - token_family_id
        - user_agent
        - client_ip
        - device_name
        - expires_at
        - last_seen_at
        - created_at
      properties:
        id:
          type: string
        user_id:
          type: string
        status:
          type: string
          enum:
            - ACTIVE
            - INACTIVE
        status_reason:
          type: string
          enum:
            - ACTIVE
            - REVOKED
            - ROTATED
            - REUSED
            - EXPIRED
        refresh_token_hash:
          type: string
        token_family_id:
          type: string
        parent_session_id:
          type:
            - string
            - 'null'
        user_agent:
          type: string
        client_ip:
          type: string
        device_name:
          type: string
        expires_at:
          type: string
          format: date-time
        last_seen_at:
          type: string
          format: date-time
        rotated_at:
          type:
            - string
            - 'null'
          format: date-time
        revoked_at:
          type:
            - string
            - 'null'
          format: date-time
        reuse_detected_at:
          type:
            - string
            - 'null'
          format: date-time
        created_at:
          type: string
          format: date-time
    UserListResponse:
      allOf:
        - $ref: '#/components/schemas/APIResponse'
        - type: object
          properties:
            data:
              type: object
              required:
                - total_count
                - page
                - page_size
                - items
              properties:
                total_count:
                  type: integer
                page:
                  type: integer
                page_size:
                  type: integer
                items:
                  type: array
                  items:
                    $ref: '#/components/schemas/AdminUser'
    UserSessionListResponse:
      allOf:
        - $ref: '#/components/schemas/APIResponse'
        - type: object
          properties:
            data:
              type: object
              required:
                - user_id
                - count
                - items
              properties:
                user_id:
                  type: string
                count:
                  type: integer
                items:
                  type: array
                  items:
                    $ref: '#/components/schemas/AdminUserLoginSession'
    UserSessionRevokeResponse:
      allOf:
        - $ref: '#/components/schemas/APIResponse'
        - type: object
          properties:
            data:
              type: object
              properties:
                user_id:
                  type: string
                session_id:
                  type: string
                token_family_id:
                  type: string
                revoked:
                  type: boolean
                revoked_count:
                  type: integer
                operation_type:
                  type: string
